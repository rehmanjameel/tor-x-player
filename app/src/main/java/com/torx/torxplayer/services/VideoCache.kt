package com.torx.torxplayer.services

import android.net.Uri

object VideoCache {
    var videoList: List<Uri> = emptyList()
    var videoPathList: List<String> = emptyList()
    var videoTitleList: List<String> = emptyList()
}
